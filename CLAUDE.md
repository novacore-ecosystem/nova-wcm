# OpenWolf

@.wolf/OPENWOLF.md

This project uses OpenWolf for context management. Read and follow .wolf/OPENWOLF.md every session. Check .wolf/cerebrum.md before generating code. Check .wolf/anatomy.md before reading files.


# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

`nova-wcm` is a Web Content Management module (for business websites, landing pages, product catalogs, content, media, and SEO) within the `nova-core` monorepo's `frontend` workspace. The repository currently contains no source code — only `README.md` and `.gitignore`. The `.gitignore` is Next.js-flavored, indicating this will be a Next.js application once scaffolded.

There are no build, lint, or test commands yet since no `package.json` or source tree exists. Update this file once the project is scaffolded and real commands/architecture exist to document.

## Related repos

Sibling frontend projects referenced in this environment (separate repos/packages, not part of this repo):
- `frontend/common/frontend-foundation` — shared authorization/permissions and i18n resources (`en`, `vi`, `zh-CN`)
- `frontend/nova-console` — a related admin console app (e.g. `(admin)/security` routes)
