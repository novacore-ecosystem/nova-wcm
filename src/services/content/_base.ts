/** Gateway prefix for Content Service REST routes (`YarpApiGateway/appsettings.json` → `Path: "/api/content/"`), same convention as every other service's `_base.ts`. */
export const BASE_PATH = "/content";
