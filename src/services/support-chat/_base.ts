/** Gateway prefix for Chat Service REST routes — mirrors Chat.API's own Swagger prefix (`/api/chat`), same convention as every other service's `_base.ts` (see `services/auth/_base.ts`). */
export const BASE_PATH = "/chat";
