/** Gateway prefix for Notification Service REST routes (`YarpApiGateway/appsettings.json` → `Path: "/api/notification/"`), same convention as every other service's `_base.ts`. */
export const BASE_PATH = "/notification";
